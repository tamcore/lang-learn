services:
  app:
    image: lang-learn:latest
    ports: ["8080:8080"]
    environment:
      JWT_SECRET: ${JWT_SECRET}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY}
      DATA_DIR: /data
    volumes:
      - lang-learn-data:/data
    restart: unless-stopped

volumes:
  lang-learn-data:
